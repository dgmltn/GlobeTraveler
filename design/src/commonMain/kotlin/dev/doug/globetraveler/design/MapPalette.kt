package dev.doug.globetraveler.design

import androidx.compose.ui.graphics.Color

/** Colors for map region fills, chosen to sit well on the muted Positron basemap. */
object MapPalette {
    val visitedFill = Color(0xFF2E7D32)
    val visitedOutline = Color(0xFF1B5E20)
    val unvisitedFill = Color(0x14000000)
    val border = Color(0xFF757575)
}
